name: schedule-echo

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Print heartbeat
        run: |
          echo "heartbeat: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
